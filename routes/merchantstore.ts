import { NextFunction, Request, Response, Router } from "express";

const router = Router();

router.use('/load', (req: Request, res: Response, next: NextFunction) => {
    
});

router.use('/save', (req: Request, res: Response, next: NextFunction) => {

});

export default router;